#ifndef _HPP_StringUtils_GetFirstNullChar_
#define _HPP_StringUtils_GetFirstNullChar_

#include <cstddef>

    char*			GetFirstNullCharPointer     (const char* s);
    std::size_t		GetFirstNullCharIndex       (const char* s);

#endif
